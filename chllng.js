// //Given an array of numbers remove all the duplicated numbers

// // removes all the publicated numbers and returns a new array of all numbers but not duplicated
// let num = [1, 2, 4, 5, 6, 1, 0, 7, 8, 9, 4];

// //you can do this
// let unique = [...new Set(num)];
// console.log(unique);

// //or this
// let uni = num.filter((itm, idx) => num.indexOf(itm) === idx);
// console.log(uni);
